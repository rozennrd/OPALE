services:
  db:
    build: .
    container_name: opale-database
    env_file:
      - .env
    ports:
      - "5432:5432"              # Expose PostgreSQL sur le port local 5432
    volumes:
      - postgres_data:/var/lib/postgresql/data   # Persistance des données

volumes:
  postgres_data:
